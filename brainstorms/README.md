# brainstorms/

Every `grill-me` session and discovery capture lands here as a dated markdown file
(`YYYY-MM-DD-topic.md`). One predictable home — captures never scatter into project folders.
If a session produces a polished deliverable, that can move to `projects/`; the raw capture
stays here.
