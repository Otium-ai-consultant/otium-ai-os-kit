# references/

Reusable knowledge: your writing voice, frameworks you follow, and notes on how each
connected tool's API works.

Expected files:
- `voice.md` — pasted samples of how you write (created by `onboard` from Q2)
- `{tool}-api.md` — one per connected tool, written when you wire it up

Link notes into the graph with `[[wikilinks]]`.
