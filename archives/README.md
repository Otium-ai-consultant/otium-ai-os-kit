# archives/

Old stuff. Superseded files, past intakes, finished projects. **Move here — never delete.**
`onboard` backs up prior intake/context files to `archives/intake-{timestamp}/` when you
re-run it, so nothing is ever lost.
